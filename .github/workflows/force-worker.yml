name: APK Worker Trigger
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: write
jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - run: echo "APK worker trigger active"
